Parameters
==========
DiscreteParameter(name, values, description) // ensure all values share the same type
ContinuousParameter(name, min_value, max_value, inclusive, description)


ValueRange()
DiscreteValueRange()
ContinuousValueRange(min_value, max_value, inclusive=False)

State Variables
===============

InternalVariable(name, description)
  * discrete vs. continuous
